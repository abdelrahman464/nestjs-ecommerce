/**
 * Ceiling per cart line, independent of live stock — anti-hoarding / sane UI
 * bound. Real stock limits are enforced separately via ReservationsService.
 */
export const CART_MAX_ITEM_QUANTITY = 50;
